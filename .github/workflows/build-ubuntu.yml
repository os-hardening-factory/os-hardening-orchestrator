name: 🐧 Ubuntu Hardened Image Build

on:
  workflow_dispatch:

jobs:
  build-ubuntu:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write

    steps:
      - name: 🧾 Checkout orchestrator repo
        uses: actions/checkout@v4

      - name: 🔐 Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::661539128717:role/GitHubActionsFactoryRole
          aws-region: ap-south-1

      - name: 🔧 Install dependencies
        run: |
          echo "🔧 Installing dependencies (Packer, Trivy, Python)..."

          # Update base system
          sudo apt-get update -y
          sudo apt-get install -y wget curl unzip python3-pip software-properties-common gpg lsb-release

          # ✅ Install Packer
          wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com noble main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
          sudo apt-get update -y && sudo apt-get install -y packer

          # ✅ Check Docker availability (use preinstalled version in runner)
          if ! command -v docker &> /dev/null; then
            echo "⚠️ Docker not found — installing minimal docker.io..."
            sudo apt-get install -y docker.io
          fi
          sudo systemctl enable docker || true
          sudo systemctl start docker || true
          docker version || (echo "❌ Docker not working!" && exit 1)

          # ✅ Install Trivy securely without containerd.io
          echo "📦 Installing Trivy..."
          curl -fsSL https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo gpg --dearmor -o /usr/share/keyrings/trivy.gpg
          echo "deb [signed-by=/usr/share/keyrings/trivy.gpg] https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main" | sudo tee /etc/apt/sources.list.d/trivy.list
          sudo apt-get update -y
          sudo apt-get install -y trivy || (echo "⚠️ Trivy not available in cache, skipping..." && true)

          # ✅ Python dependencies
          pip3 install boto3 PyGithub

          echo "✅ Dependencies installed successfully."



      - name: 🧾 Verify Ansible Files
        run: |
          echo "Verifying Ansible files for Ubuntu..."
          ls -R packer/ubuntu/ansible || (echo "❌ Missing Ansible files!" && exit 1)

      - name: 🏗️ Build hardened Ubuntu image
        env:
          PACKER_LOG: "1"
        run: |
          echo "🚀 Building Ubuntu CIS 1.4 Hardened Image..."
          packer init packer/ubuntu
          packer validate packer/ubuntu
          packer build \
            -var "os_name=ubuntu" \
            -var "os_version=22.04" \
            -var "cis_version=1.4" \
            -var "timestamp=$(date +%Y%m%d)" \
            packer/ubuntu

      - name: 🔍 Scan image using Trivy
        run: |
          echo "🔍 Scanning hardened image..."
          IMAGE_TAG=$(date +%Y%m%d)
          IMAGE_NAME="ubuntu-22.04-cis1.4-hardening-${IMAGE_TAG}"
          trivy image ${IMAGE_NAME}:latest -f json -o reports/trivy-${IMAGE_NAME}.json || true

      - name: ☁️ Upload compliance report to S3
        run: |
          echo "☁️ Uploading compliance report..."
          python3 tools/upload_to_s3_and_trigger_glue.py
        env:
          AWS_REGION: ap-south-1

      - name: ✅ Summary
        run: |
          IMAGE_TAG=$(date +%Y%m%d)
          IMAGE_NAME="ubuntu-22.04-cis1.4-hardening-${IMAGE_TAG}"
          echo "### ✅ Ubuntu Hardened Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "- Image Name: \`${IMAGE_NAME}\`" >> $GITHUB_STEP_SUMMARY
          echo "- CIS Version: 1.4" >> $GITHUB_STEP_SUMMARY
          echo "- Date: $(date)" >> $GITHUB_STEP_SUMMARY
