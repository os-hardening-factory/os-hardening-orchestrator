name: 🚀 Promote Hardened Image to Prod (Security Gating + Release Publishing + Asset Upload)

on:
  workflow_dispatch:
    inputs:
      vendor:
        description: "OS vendor (e.g., ubuntu, rhel, amazonlinux)"
        required: true
        default: "ubuntu"
      source_tag:
        description: "Existing tag to promote (e.g., ubuntu-22.04-intuit-1)"
        required: true
      target_tag:
        description: "New promotion tag (e.g., ubuntu-22.04-intuit-prod)"
        required: true
        default: "ubuntu-22.04-intuit-prod"
      cis_profile:
        description: "CIS profile version (e.g., cis1.4)"
        required: true
        default: "cis1.4"
      version:
        description: "OS version (e.g., 22.04)"
        required: true
        default: "22.04"

jobs:
  promote:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write

    env:
      AWS_REGION: ap-south-1
      ACCOUNT_ID: "661539128717"

    steps:
      - name: 🔐 Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::661539128717:role/GitHubActionsFactoryRole
          aws-region: ${{ env.AWS_REGION }}

      - name: 🧾 Checkout os-hardening-orchestrator repository
        uses: actions/checkout@v4
        with:
          repository: os-hardening-factory/os-hardening-orchestrator
          fetch-depth: 0  # ensures full git history & .git folder


      - name: 🧾 Log input parameters
        run: |
          echo "Vendor        : ${{ github.event.inputs.vendor }}"
          echo "Source tag    : ${{ github.event.inputs.source_tag }}"
          echo "Target tag    : ${{ github.event.inputs.target_tag }}"
          echo "CIS profile   : ${{ github.event.inputs.cis_profile }}"
          echo "Version       : ${{ github.event.inputs.version }}"

      # ---------- Step 1: Trivy installation ----------
      - name: 🔍 Install Trivy
        run: |
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh \
            | sh -s -- -b /usr/local/bin
          trivy --version

      # ---------- Step 2: Security Gating ----------
      - name: 🛡️ Security Gate (Trivy CVE check)
        env:
          VENDOR: ${{ github.event.inputs.vendor }}
          SOURCE_TAG: ${{ github.event.inputs.source_tag }}
          ACCOUNT_ID: ${{ env.ACCOUNT_ID }}
          AWS_REGION: ${{ env.AWS_REGION }}
          BLOCK_SEVERITIES: "CRITICAL,HIGH"
        run: |
          set -euo pipefail
          REPO="${ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/hardened-${VENDOR}"
          IMG="${REPO}:${SOURCE_TAG}"
          aws ecr get-login-password --region ${AWS_REGION} | docker login --username AWS --password-stdin ${REPO}
          docker pull "${IMG}"

          mkdir -p reports
          trivy image --timeout 5m --severity ${BLOCK_SEVERITIES} \
            --format json --output "reports/trivy-${VENDOR}-${SOURCE_TAG}.json" "${IMG}" || true

          CRIT=$(jq '[.Results[].Vulnerabilities[]? | select(.Severity=="CRITICAL")] | length' reports/trivy-${VENDOR}-${SOURCE_TAG}.json)
          HIGH=$(jq '[.Results[].Vulnerabilities[]? | select(.Severity=="HIGH")] | length' reports/trivy-${VENDOR}-${SOURCE_TAG}.json)
          echo "CRITICAL=${CRIT} HIGH=${HIGH}"
          if [ "${CRIT}" -gt 0 ] || [ "${HIGH}" -gt 0 ]; then
            echo "❌ Blocked due to critical vulnerabilities"
            exit 1
          fi
          echo "✅ Passed security gate"

      # ---------- Step 3: Promotion ----------
      - name: 🧱 Retag and push image (Prod)
        if: success()
        run: |
          VENDOR=${{ github.event.inputs.vendor }}
          SRC=${{ github.event.inputs.source_tag }}
          DST=${{ github.event.inputs.target_tag }}
          REPO="${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/hardened-${VENDOR}"
          aws ecr get-login-password --region ${{ env.AWS_REGION }} | docker login --username AWS --password-stdin ${REPO}
          docker pull ${REPO}:${SRC}
          docker tag  ${REPO}:${SRC} ${REPO}:${DST}
          docker push ${REPO}:${DST}
          echo "✅ Promoted ${REPO}:${SRC} → ${REPO}:${DST}"

      - name: 📊 Upload Trivy report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trivy-${{ github.event.inputs.vendor }}-${{ github.event.inputs.source_tag }}
          path: reports/
          retention-days: 30

      # ---------- Step 4: Generate metadata for release ----------
      - name: 🧾 Generate release metadata and changelog entry
        id: metadata
        if: success()
        run: |
          DATE=$(date +%Y%m%d)
          COMMIT=$(git rev-parse --short HEAD || echo "manual")
          ECR_IMAGE="${{ env.ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/hardened-${{ github.event.inputs.vendor }}:${{ github.event.inputs.target_tag }}"
          SUMMARY="Image promoted to prod after passing security validation. CRITICAL/HIGH CVEs = 0."
          echo "DATE=${DATE}" >> $GITHUB_ENV
          echo "COMMIT=${COMMIT}" >> $GITHUB_ENV
          echo "ECR_IMAGE=${ECR_IMAGE}" >> $GITHUB_ENV
          echo "SUMMARY=${SUMMARY}" >> $GITHUB_ENV

      - name: 🐍 Install Python dependencies for GitHub release dispatch
        run: |
          python3 -m pip install --upgrade pip
          pip install PyGithub


      - name: 🚀 Dispatch release to vendor repo
        if: success()
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        run: |
          echo "📂 Current working directory: $(pwd)"
          echo "🔍 Verifying tools directory..."
          ls -R tools || (echo "❌ tools directory not found!" && exit 1)

          # Move to correct workspace (ensures relative paths work)
          cd $GITHUB_WORKSPACE

          echo "🧠 Running release dispatch script..."
          python3 tools/dispatch_to_vendor_repos.py \
            --vendor "${{ github.event.inputs.vendor }}" \
            --cis_profile "${{ github.event.inputs.cis_profile }}" \
            --version "${{ github.event.inputs.version }}" \
            --build_date "${{ env.DATE }}" \
            --summary "${{ env.SUMMARY }}"

          echo "✅ Release successfully created in vendor repo"


      # ---------- Step 6: Attach Trivy report to GitHub release ----------
      - name: 📎 Attach Trivy report to vendor release
        if: success()
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          VENDOR: ${{ github.event.inputs.vendor }}
        run: |
          RELEASE_TAG="${{ github.event.inputs.version }}-${{ github.event.inputs.cis_profile }}-${{ env.DATE }}"
          FILE_PATH="reports/trivy-${VENDOR}-${{ github.event.inputs.source_tag }}.json"
          REPO="os-hardening-${VENDOR}"

          echo "🔗 Uploading ${FILE_PATH} to release ${RELEASE_TAG} in ${REPO}..."
          gh release upload "${RELEASE_TAG}" "${FILE_PATH}" \
            --repo "os-hardening-factory/${REPO}" --clobber
          echo "✅ Attached Trivy report as release asset"

      - name: 🧾 Final promotion summary
        if: success()
        run: |
          echo "Promotion Summary:"
          echo "  Vendor     : ${{ github.event.inputs.vendor }}"
          echo "  Target Tag : ${{ github.event.inputs.target_tag }}"
          echo "  ECR Image  : ${{ env.ECR_IMAGE }}"
          echo "  Release    : ${{ github.event.inputs.version }}-${{ github.event.inputs.cis_profile }}-${{ env.DATE }}"
          echo "  Attached   : Trivy report asset"
      
            - name: 🧾 Generate final audit summary
        if: always()
        run: |
          echo "✅ Hardened Image Promotion Summary" > summary.md
          echo "----------------------------------" >> summary.md
          echo "🧩 Vendor:        ${{ github.event.inputs.vendor }}" >> summary.md
          echo "🏷️  Source Tag:   ${{ github.event.inputs.source_tag }}" >> summary.md
          echo "🚀 Target Tag:    ${{ github.event.inputs.target_tag }}" >> summary.md
          echo "🔖 Release:       ${{ github.event.inputs.version }}-${{ github.event.inputs.cis_profile }}-${{ env.DATE }}" >> summary.md
          echo "🛡️  CVE Status:   PASSED (0 CRITICAL / 0 HIGH)" >> summary.md
          echo "📦 ECR Image:     ${{ env.ECR_IMAGE }}" >> summary.md
          echo "🧾 Report:        trivy-${{ github.event.inputs.vendor }}-${{ github.event.inputs.source_tag }}.json" >> summary.md
          echo "💾 Commit:        ${{ env.COMMIT }}" >> summary.md
          echo "📅 Date:          $(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> summary.md
          echo "" >> summary.md
          echo "✅ Promotion job completed successfully and release published to vendor repo." >> summary.md

          cat summary.md

      - name: 📤 Upload audit summary
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: audit-summary-${{ github.event.inputs.vendor }}
          path: summary.md
          retention-days: 30


