name: 🧩 OS Hardening Controller

on:
  # ✅ Manual trigger only
  workflow_dispatch:
    inputs:
      vendor:
        description: "Select OS vendor (amazonlinux | ubuntu | rhel | all)"
        required: true
        default: "all"

# ✅ No push or PR events defined at all
# Prevents any auto-triggering when the file itself changes

concurrency:
  group: "controller-${{ github.ref }}"
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  build-all:
    if: ${{ github.event.inputs.vendor == 'all' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        vendor: [ubuntu, amazonlinux, rhel]
    steps:
      - name: ▶️ Call vendor workflow - ${{ matrix.vendor }}
        uses: ./.github/workflows/build-${{ matrix.vendor }}.yml

  build-one:
    if: ${{ github.event.inputs.vendor != 'all' }}
    runs-on: ubuntu-latest
    steps:
      - name: ▶️ Call vendor workflow - ${{ github.event.inputs.vendor }}
        uses: ./.github/workflows/build-${{ github.event.inputs.vendor }}.yml
